---
- name: Reload systemd daemon (egress-gateway)
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart mando-gateway
  ansible.builtin.systemd_service:
    name: mando-gateway
    state: restarted
