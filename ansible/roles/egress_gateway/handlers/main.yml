---
- name: Reload systemd daemon (egress-gateway)
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart fleet-gateway
  ansible.builtin.systemd_service:
    name: fleet-gateway
    state: restarted
