---
- name: Reload systemd daemon (mando-agent)
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart mando-agent
  ansible.builtin.systemd_service:
    name: mando-agent
    state: restarted
