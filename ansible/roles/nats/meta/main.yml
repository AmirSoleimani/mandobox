---
galaxy_info:
  author: Chelodo
  description: Deploy a NATS server on the control plane (minimal; per-session JWT scoping added later).
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
    - name: Ubuntu
      versions:
        - jammy
        - noble
  galaxy_tags:
    - fleet
    - nats
dependencies: []
