# Go build output
/bin/
/dist/
*.test
*.out
dashboard/dashboard
# Built binaries sometimes left at the repo root by `go build` (the real outputs live in bin/)
/mando-worker
/mando-reconciler
/mando-agent
/mando-gateway
/mando-dispatch
/mando-natsauth
/nats-bridge
/nats-sub
/slack-gateway
/telegram-gateway
/webhook-rx
/fc-supervisor

# Internal planning doc — kept locally, never published to the public repo
docs/PLAN.md

# Python / tooling venvs
.venv/
__pycache__/

# Ansible
*.retry
ansible/*.log
ansible/fact_cache/

# Local secrets / inventories (never commit real hosts, keys, or PKI)
ansible/inventory/local.yml
ansible/inventory/*.local.yml
secrets/*
!secrets/README.md
*.vault
.vault_pass
*.key
*.pem
*.crt
*.csr
*.srl

# Large downloaded artifacts (kernels, rootfs images) — reconstructed by the playbook
*.ext4
*.ext4.zst
*.squashfs
vmlinux*
initramfs*
*.tgz
*.tar.zst

# Editor / OS
.DS_Store
.idea/
.vscode/
